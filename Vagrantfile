ENV['DRUPALVM_PROJECT_ROOT'] = "#{__dir__}"
ENV['DRUPALVM_CONFIG_DIR'] = "config"
ENV['DRUPALVM_DIR'] = "vendor/oxyc/drupal-vm"
# Load the real Vagrantfile
load "#{__dir__}/vendor/oxyc/drupal-vm/Vagrantfile"
